/* 
import postLogin from "./Axios";

postLogin = async() => {
    const data = await postLogin(Response);
    //오는 응답의 형식에 따라 date ~를 변경한다.
    if(data.complete){
        const  = data.message.id;

    }else {
        //error
        return 'fail';
    }
};
export default postLogin; */